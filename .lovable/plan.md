

## Change Rate Input Step to $1.00

### Change — 1 File: `src/components/pms/BulkRestrictionEditor.tsx`

**Line 524:** Change `step={0.01}` to `step={1}` on the Rate number input. Users can still manually type decimal values — only the stepper arrows change behavior.

